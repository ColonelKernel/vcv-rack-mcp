{
  "name": "rack-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Rack MCP — a safe local Model Context Protocol integration for VCV Rack 2.6.6",
  "license": "SEE LICENSE IN LICENSE.md",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/**' --filter './apps/**' run build",
    "test": "pnpm -r run test",
    "gen": "pnpm --filter @rackmcp/schemas run build && tsx scripts/gen-schemas.ts && tsx scripts/gen-cpp.ts",
    "check:gen": "pnpm run gen && git diff --exit-code -- packages/schemas/json plugins/RackMCP/src/gen",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "prettier": "^3.9.6",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "zod": "4.4.3"
  }
}
